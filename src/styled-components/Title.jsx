import styled from 'styled-components';

const Title = styled.h2`
  font-size: 3rem;
  line-height: 3.6rem;
  @media (min-width: 920px) {
    font-size: 4.3rem;
    line-height: 5rem;
  }
`;

export default Title;